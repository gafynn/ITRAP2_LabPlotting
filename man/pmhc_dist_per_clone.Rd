% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting_new.R
\name{pmhc_dist_per_clone}
\alias{pmhc_dist_per_clone}
\title{Distribution of pMHC per Clone}
\usage{
pmhc_dist_per_clone(
  object,
  clone,
  aggr_threshold = 10,
  slot = "counts",
  xlimits = NULL,
  return_pmhcs = F,
  display_pvalues = F,
  conc_fontsize = 8,
  dot_fontsize = 8,
  hm_fontsize = 8,
  rel_height1 = 0.05,
  extra_title = NULL,
  heatmap_legend_side = "left",
  hm_legent_direction = "vertical",
  ...
)
}
\arguments{
\item{object}{A Seurat object containing the clonotype and pMHC information.}

\item{clone}{The identifier for the clone to be analyzed within the dataset.}

\item{aggr_threshold}{A numeric value specifying the minimum aggregate count threshold for pMHCs to be included in the analysis.
Default is 10.}

\item{slot}{The assay slot to use for obtaining pMHC data. Common values include 'counts', 'data', or 'scale.data'.
Default is 'counts'.}

\item{xlimits}{An optional numeric vector of length 2 specifying the x-axis limits for the jitter plots. If NULL (default),
x-axis limits are determined automatically.}
}
\value{
A cowplot object combining a heatmap of pMHC distribution, a jitter plot of UMI counts by feature, and a bar plot
of concordance values for the pMHCs analyzed. This comprehensive visualization aids in understanding the distribution and
concordance of pMHCs per clone.
}
\description{
This function plots a distribution of pMCH with some extra matrix within a specific clone, visualizing the distribution using
a heatmap and jitter plots. The function aims to provide insights into the diversity and abundance of pMHCs associated
with a particular clone, considering only pMHCs that surpass a specified aggregation threshold.
}
\examples{
# Assuming 'data' is a Seurat object with pMHC data and clone identifiers:
clone_analysis_plot <- pmhc_dist_per_clone(data, clone = "clone1")
print(clone_analysis_plot)

}
