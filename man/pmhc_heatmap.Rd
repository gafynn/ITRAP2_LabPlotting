% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting_new.R
\name{pmhc_heatmap}
\alias{pmhc_heatmap}
\title{Generate a Heatmap of pMHC Distribution Across Clones}
\usage{
pmhc_heatmap(
  object,
  clones,
  patient = NULL,
  slot = "counts",
  assay = "pMHC",
  clones_order = NULL,
  column_split_var = NULL,
  split_rows = NULL,
  highlight_pmhc.tcr = F,
  hm_breaks = NULL,
  hm_palette = c("blue", "white", "red"),
  clean_na_features = F,
  na_col = "grey",
  pmhc_palette = NULL,
  pmhc_order = NULL,
  highlight_column = "pMHC_classification",
  highlight_color = "green",
  stop_large_highlighting = T,
  show_heatmap_legend = T,
  rowm.fonts = 8,
  column_title_fonts = 10,
  column_title_rot = 45,
  clean_mat = F,
  add_tcr_cluster = F,
  show_row_names = T,
  pmhc_subset = NULL,
  clean_na_cells = FALSE,
  custom_annotations = c(),
  custom_ann_palette = list(),
  show_legend_ann = FALSE,
  bugged_width = 0.6,
  max_cols = 16000,
  left_ann_vars = NULL,
  left_ann_palette = NULL,
  save_to_disc_highlight = F,
  verbose = T,
  lwd = 2,
  flip = FALSE,
  skip_bugged_frames = F,
  ...
)
}
\arguments{
\item{object}{Seurat object, containing single-cell data along with
pMHC and clone annotations.}

\item{clones}{A vector of clone identifiers to include in the analysis.}

\item{patient}{Optional; a patient identifier to filter the data by a specific patient.}

\item{slot}{The assay slot from which to retrieve the data (default is 'counts').}

\item{clones_order}{Optional; an order for the clones to be displayed in the heatmap.}

\item{highlight_pmhc.tcr}{Optional; specific pMHC-TCR interactions to highlight in the heatmap.}

\item{hm_breaks}{Numeric vector specifying the breakpoints for heatmap coloring.}

\item{hm_palette}{A vector of colors corresponding to \code{hm_breaks} for heatmap coloring.}

\item{pmhc_palette}{A color palette for pMHCs, if differentiating by pMHC is desired.}

\item{rowm.fonts}{Font size for row names.}

\item{column_title_fonts}{Font size for the column title.}

\item{column_title_rot}{Rotation angle for the column title.}

\item{clean_mat}{Logical; whether to remove 'Negative' classifications from the matrix.}

\item{add_tcr_cluster}{Logical; whether to add TCR clustering information, if available.}

\item{show_row_names}{Logical; whether to display row names in the heatmap.}

\item{pmhc_subset}{Optional; a subset of pMHCs to include in the heatmap.}

\item{lwd}{thickness of the highliting frame}

\item{...}{Additional arguments passed to the heatmap function.}

\item{condpalette}{A color palette for conditioning variables, if applicable.}

\item{use_original_order}{Logical; whether to use the original order of clones.}
}
\value{
A ComplexHeatmap object visualizing the specified pMHC distribution across clones. The heatmap
can be customized extensively via function arguments and supports highlighting of specific pMHC-TCR interactions.
}
\description{
This function visualizes calues of peptide-MHC (pMHC) across different clones
within a given dataset, offering the option to highlight specific pMHC-TCR pairs. It allows for
customization of the heatmap appearance and can subset the data based on patient identifiers or specific
pMHCs. The heatmap can be ordered based on various criteria to facilitate the identification of patterns
or relationships within the data.
}
\examples{
# Assuming 'data' is a dataset object with required annotations:
heatmap <- pmhc_heatmap(data, clones = c("clone1", "clone2"), patient = "patient1")
print(heatmap)

}
